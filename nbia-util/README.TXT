The classes in this project should have MINIMAL dependencies and encapsulate
simple logic that is reusable across MOST or ALL other NBIA projects.  This
should be things like String manipulation logic, collection manipulation logic,
etc.